# Test generated by RoostGPT for test flightReservation using AI Type Open AI and AI Model gpt-4

config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/admin/showAddFlight"
          expect:
            - statusCode: 200
            - hasProperty: "flights/addFlight"
            
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/admin/showAddFlight"
          headers:
            Invalid-Header: "Invalid value"
          expect:
            - statusCode: 400
            
  - name: "API request with edge case"
    flow:
      - get:
          url: "/admin/showAddFlight?param=@@@@@"
          expect:
            - statusCode: 200
            - hasProperty: "flights/addFlight"
